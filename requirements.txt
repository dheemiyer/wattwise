# WattWise runtime + test dependencies (pinned for reproducible installs).
# Install with either:
#   uv pip install -r requirements.txt
#   pip install -r requirements.txt
fastapi==0.138.2
uvicorn[standard]==0.49.0
jinja2==3.1.6
httpx==0.28.1
python-dotenv==1.2.2
python-multipart==0.0.32
pytest==9.1.1
